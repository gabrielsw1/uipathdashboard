# Orchestrator Configuration
ORCHESTRATOR_URL=https://cloud.uipath.com
ORCHESTRATOR_TENANT=seu-tenant
# Organization geralmente é igual ao tenant, mas pode ser diferente
# Se não especificado, usa o mesmo valor do tenant
ORCHESTRATOR_ORGANIZATION=seu-organization

# Autenticação - Use UM dos métodos abaixo:
# Método 1: Personal Access Token (recomendado)
ORCHESTRATOR_PAT=seu-personal-access-token

# Método 2: OAuth2 Client Credentials (alternativa)
# ORCHESTRATOR_CLIENT_ID=your-client-id
# ORCHESTRATOR_CLIENT_SECRET=your-client-secret

# Server Configuration
PORT=3001
NODE_ENV=development

# Cache Configuration
CACHE_TTL=300

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

